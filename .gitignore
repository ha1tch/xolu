# Sandbox-only artefacts, not repo-tracked upstream either
tests/fuzz/corpus/
tests/fuzz/crashers/

# Build artifacts (produced by release.sh / go build) — never commit
/xolu
/iolu
/xotogen
bin/

# Test and coverage artifacts (regenerable)
test-output.json
test-errors.txt
cover.out
cover-summary.txt
